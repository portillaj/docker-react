sudo: required
services:
  - docker

before_install:
  - docker build -t jasonportilla/docker-react -f dockerfile.dev .

script:
  - docker run -e CI=true jasonportilla/docker-react npm run test

language: generic